import { Meta, ColorPalette, ColorItem } from "@storybook/blocks";
import * as tokens from "@esnet/esnet-tokens/tokens.json";
import { TypographyDocBlock } from "doc-ui";

<Meta title="Design Tokens/Typography" />

#### Design Tokens

# Typography

## Fonts

### Tokens

## Typeramps/Typesets

### Tokens

{
    Object.keys(tokens.typography).map(typographyKey => {
        return(<TypographyDocBlock name={`--esnet-typography-${typographyKey}`} value={tokens.typography[typographyKey].value || 0} />)
    })
}
