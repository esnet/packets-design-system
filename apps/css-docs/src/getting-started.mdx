import { Meta } from "@storybook/blocks";

<Meta title="About/Getting Started" />

<img src="/imgs/packetslogo.light.lrg.png" alt="Packets Design System" />

<div className="sb-container">
  <div className='sb-section-title'>
    # About

    Packets is a design system created as a cross organizational collaboration at ESnet. It is intended to be a source of truth for common UI/UX patterns. It leverages a design tokens powered system and provides pure HTML/CSS components with no JavaScript dependencies, perfect for static sites, server-rendered applications, and projects without a build system.

  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # Installation

    ## Via NPM/PNPM/Yarn

    Install the package:

```bash
npm install @esnet/packets-ui-css
```

    Import the CSS in your project:

```css
@import "@esnet/packets-ui-css/dist/styles.css";
```

    ## Via CDN (No Build System)

    For projects without a build system, load from unpkg:

```html
<!DOCTYPE html>
<html>
<head>
  <!-- Packets CSS -->
  <link rel="stylesheet" href="https://unpkg.com/@esnet/packets-ui-css@latest/dist/styles.css" />

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Signika:wght@300..700&family=Source+Code+Pro:ital,wght@0,200..900;1,200..900&family=Source+Sans+3:ital,wght@0,200..900;1,200..900&display=swap" rel="stylesheet" />
</head>
<body class="packets">
  <button class="pkts-button pkts-primary">Click Me</button>
</body>
</html>
```

  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # Fonts

    Add the following to your `<head>` element to load the Packets fonts from Google's CDN:

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Signika:wght@300..700&family=Source+Code+Pro:ital,wght@0,200..900;1,200..900&family=Source+Sans+3:ital,wght@0,200..900;1,200..900&display=swap" rel="stylesheet" />
```

  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # Usage

    1. Add the `packets` class to your root container:

```html
<div class="packets">
  <!-- Your components here -->
</div>
```

    2. Use component classes as shown in the component examples:

```html
<div class="packets">
  <button class="pkts-button pkts-primary">Primary Button</button>

  <div class="pkts-input-text">
    <input type="text" placeholder="Enter text..." />
  </div>

  <div class="pkts-alert pkts-info">
    <aside class="icon">
      <!-- Icon SVG here -->
    </aside>
    <div class="content">
      <strong>Info Alert</strong>
      <p>This is an informational message.</p>
    </div>
  </div>
</div>
```

  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # Theme

    Add `dark` or `light` classes to your root container to force a specific theme:

```html
<div class="packets dark">
  <!-- Dark mode components -->
</div>
```

  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # Browse Components

    Use the sidebar to browse interactive examples of all CSS components. Each story includes the HTML markup and CSS classes you can copy and use in your project.

  </div>
</div>
